#ADD CURRENT FOLDER IN INCLUDE PATH
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

#ADD ALL C FILES IN CURRENT FOLDER IN SRC_FILES
#DONT ADD main.c
FILE(GLOB SRC_FILES *.c)
LIST(FILTER SRC_FILES EXCLUDE REGEX "main.c$")

#GENERATE SHARED OBJECT LIBRARY
ADD_LIBRARY(c_bmp SHARED ${SRC_FILES})